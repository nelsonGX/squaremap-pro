# `next dev` defaults to the in-memory fixture API (no server needed).
# Override in .env.development.local (NEXT_PUBLIC_USE_FIXTURES=0, NEXT_PUBLIC_API_BASE, NEXT_PUBLIC_TILES_BASE).
NEXT_PUBLIC_USE_FIXTURES=1
